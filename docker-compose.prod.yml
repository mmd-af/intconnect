services:
  php:
    build:
      context: .
      dockerfile: ./docker/prod/Dockerfile
      args:
        UID: 1000
        GID: 1000
    container_name: intconnect-php-prod
    restart: unless-stopped
    depends_on:
      database:
        condition: service_healthy
    env_file:
      - ./src/.env.prod
    environment:
      TZ: Europe/Bucharest
    working_dir: /var/www
    volumes:
      - './src:/var/www'
      - /var/www/vendor
      - /var/www/public/build
    networks:
      - intconnect-prod

  nginx:
    build:
      context: .
      dockerfile: ./docker/nginx/Dockerfile
    container_name: intconnect-nginx-prod
    restart: unless-stopped
    depends_on:
      - php
    ports:
      - '80:80'
    environment:
      FPM_HOST: "php:9000"
      TZ: Europe/Bucharest
    volumes:
      - './src:/var/www'
    networks:
      - intconnect-prod

  database:
    image: mariadb:11.0
    container_name: intconnect-database-prod
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "healthcheck.sh",
          "--su-mysql",
          "--connect",
          "--innodb_initialized"
        ]
      interval: 5s
      timeout: 5s
      retries: 3
      start_period: 1m
    ports:
      - "3306:3306"
    hostname: database
    volumes:
      - "database-prod:/var/lib/mysql"
    stop_signal: SIGINT
    env_file:
      - ./src/.env.prod
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-intconnect}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_DATABASE: ${DB_DATABASE}
      TZ: Europe/Bucharest
    networks:
      - intconnect-prod

networks:
  intconnect-prod:

volumes:
  database-prod:
